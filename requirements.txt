# Django Core
Django>=4.2,<5.0
python-dotenv>=1.0.0

# Database
psycopg2-binary>=2.9.9
dj-database-url>=2.1.0

# AI & ML
google-genai>=0.3.0
groq>=0.4.0  # Ultra-fast Llama 3.1 inference (FREE during beta)

# Real-Time (Django Channels)
channels>=4.0.0
channels-redis>=4.1.0
daphne>=4.0.0

# Background Tasks (Celery)
celery>=5.3.0
redis>=5.0.0
django-celery-beat>=2.5.0
django-celery-results>=2.5.0

# File Processing
Pillow>=10.0.0
python-docx>=1.0.0
PyPDF2>=3.0.0
mammoth>=1.6.0

# Static Files & Deployment
whitenoise>=6.6.0
gunicorn>=21.0.0

# Voice/Speech (for Vernacular Voice)
SpeechRecognition>=3.10.0
gTTS>=2.4.0
# googletrans==3.1.0a0  # Disabled due to httpx conflict with groq
deep-translator>=1.11.0  # Alternative translator that doesn't conflict

# Geolocation
geopy>=2.4.1

# Security & Auth
django-cors-headers>=4.3.0
django-allauth>=0.57.0  # Social authentication (Google, GitHub)
PyJWT>=2.0.0  # Required for Google OAuth

# API & JSON
djangorestframework>=3.14.0

# Payment Gateway
razorpay>=1.4.1

# Utilities
python-dateutil>=2.8.2
